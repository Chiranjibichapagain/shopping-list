import { FunctionComponent } from "react";

import { T_HeaderProps } from "./types";
import { Container } from "../Container";

export const Header: FunctionComponent<T_HeaderProps> = (
  props: T_HeaderProps
) => {
  return <Container styles={{}}>xx</Container>;
};
